import { StopBodyScrollDirective } from './stop-body-scroll.directive';

describe('StopBodyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new StopBodyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
